# confuse 

confuse aims to become a cpq style evalutation engine that parses configuration models and provides the possibility to set values for the defined options. based on the input values and defined constraints it will evaluate output values.

# quick start
- install deno
- `deno run dev`