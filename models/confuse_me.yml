options:
  flavour:
    label: 'flavour'
    type: string
  volume:
    label: 'volume ml'
    type: number
    min: 0
    max: 100
    intervall: 1
  cup_size:
    label: 'third option'
    type: value_list
    values:
      - 'small'
      - 'medium'
      - 'large'
constraints:
  medium_cup:
    description: 'gonna need a bigger cup for that'
    if: 'volume >= 20'
    then: 'cup_size != small'
  large_cup:
    description: 'somebody is reeaaaly thirsty '
    if: 'volume >= 70'
    then: 'cup_size == large'
evaluations:
  values:
    ttl:
      description: 'time to loo'
      base_eval: '1/volume'
  rules:
    apple_rule:
      description: 'cannot hold my applejuice'
      if: 'flavor==apple'
      then: ttl/=2